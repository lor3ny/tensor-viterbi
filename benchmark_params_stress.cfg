# Stress-test sweep parameters
# Used only by `bench plan --pack stress`, which requires a GPU system.
# Single large (states, durations, timesteps) combination — see the `stress`
# pack in README.md/REPRODUCING.md.

states    = 100
durations = 10000
timesteps = 10000000
