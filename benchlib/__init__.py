"""benchlib — implementation modules behind the `bench` CLI.

Nothing in here auto-detects scheduler, system, or toolchain: every fact
about a machine comes from the YAML file passed to --system.
"""
