name: b200
type: gpu
gpu_arch: "100"
toolchain: cuda
scheduler: slurm

# No slurm account/partition configured yet for this system (matches the
# state of the old slurm_system.yaml, which never listed b200 either).
# `bench check` will warn until this is filled in.
slurm: {}
