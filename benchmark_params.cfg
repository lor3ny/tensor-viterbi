# Benchmark sweep parameters
# Edit these lists to control which (states, durations, timesteps) combinations
# are submitted. Values are comma-separated integers.

states    = 10, 15, 25, 50, 75
durations = 100, 250, 500, 1000
timesteps = 10000
